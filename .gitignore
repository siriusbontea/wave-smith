# Dependencies
node_modules/
.pnpm-store/

# Build output (anchored — only repo-root dirs)
/.next/
/out/
/dist/

# Runtime data (generated audio, sqlite db) — anchored: lib code may have data/ subdirs
/data/

# Engine clone (external pinned dependency) — anchored: lib/engine/ is app code!
/engine/

# Env
.env
.env.local

# OS / editor
.DS_Store
*.log

# Next.js / TypeScript build artifacts
*.tsbuildinfo
next-env.d.ts

# Test output
/test-results/
/playwright-report/
/blob-report/
