# DECISIONS.md — one line per judgment call (spec §0.4)

- 2026-06-12 **Build mode:** milestone-gated with user sign-off at each gate (user's explicit choice over spec §0's full autonomy).
- 2026-06-12 **Node 26.3.0 kept** (spec pins Node 22 LTS): user's call; M1 `pnpm build`/strict `tsc` gate is the compatibility check.
- 2026-06-12 **Engine pinned v0.1.8 @ `dce6214`** — latest release tag, equal to origin HEAD at clone time; tag-only fetch refspec also defuses the launcher's interactive update-check.
- 2026-06-12 **uv 0.11.21 (brew), engine deps via `uv sync`** — exact versions in engine `uv.lock`; venv 1.2 GB, torch 2.10.0, MLX importable.
- 2026-06-12 **API contract derived from source, not docs** — 10-agent map+verify workflow (raw evidence: `docs/m0-raw/*.json`); docs/en/API.md found accurate but incomplete, several documented params dead in code.
- 2026-06-12 **Bypass `start_api_server_macos.sh`** in our scripts; run `uv run acestep-api --host 127.0.0.1 --port 8001` with `ACESTEP_LM_BACKEND=mlx` directly (no interactive update prompt, no pip self-repair, deterministic env).
- 2026-06-12 **Models pre-fetched via `acestep-download`** (strict weight check) — never trust the server's lazy first-job download (loose check + blocks event loop).
- 2026-06-12 **Warm-up = lazy boot + `POST /v1/init {init_llm:true, lm_model_path:"acestep-5Hz-lm-1.7B"}`** then poll `/health.models_initialized` — keeps `/health` responsive (executor) and avoids the empty-body trap that later lazy-loads the 0.6B LM.
- 2026-06-12 **No engine API key for MVP** — localhost-only binding; `--api-key` flag is a silent no-op anyway (env `ACESTEP_API_KEY` is the working mechanism if ever needed).
- 2026-06-12 **LRC + quality score: unavailable over REST (Gradio-only)** → `songs.lrc`/`songs.quality_score` stay null; synced lyrics falls back to plain text; quality badge hidden (spec anticipated this with "verify in M0").
- 2026-06-12 **Variations = one `/release_task` with `batch_size=N`** (default 2, clamp 1–4 client-side — engine has no batch cap on MPS), per-take explicit seeds via `use_random_seed:false` (random-mode `seed_value` is unreliable per source).
- 2026-06-12 **Request `wav` masters from the engine; mp3 via our own ffmpeg encode** (engine mp3 is fixed 128k; we own download encoding per spec §9.3). Pending ffprobe bit-depth check.
- 2026-06-12 **Enhance button = `POST /format_input`; Simple-Mode plan = `sample_mode`/`sample_query` on `/release_task`** (or `/v1/create_sample` if a sync plan is ever needed); Enhance calls serialized client-side (shared LLMHandler, no queue).
- 2026-06-12 **Poll timeout owned by client** — unknown/expired task ids return status 0 forever; cap polling at engine timeout (600 s) + margin, then fail the job.
- 2026-06-12 **Engine failure UX:** engine returns no error text via `/query_result` (cache path) — jobs store a generic engine-failure message plus last `progress_text` as a hint.
- 2026-06-12 **`ACESTEP_CHECKPOINTS_DIR` not set** (API server ignores it in places → double-download risk); checkpoints live in `engine/ACE-Step-1.5/checkpoints/`.
- 2026-06-12 **Weights fetched via direct `curl` from HF resolve URLs + sha256 verify against LFS oids** — `acestep-download`'s xet-backed path stalled repeatedly (<200 KB/s, sparse preallocated blobs that defeat size-based progress checks) while the classic CDN path sustained ~3.5 MB/s on the same link; engine runtime only checks files-on-disk, so hf metadata is unnecessary. `setup.sh` will prefer `acestep-download` but document this fallback.
- 2026-06-12 **Built-in LM is NOT censored in practice:** M0 probes show the 1.7B planner writes explicit lyrics on request, and user-supplied explicit lyrics render unmodified — the spec's "safety-tuned, may refuse" assumption did not reproduce. Ollama lyrics seam (§6.2) retained for lyric *quality* (a dedicated uncensored writer > a 1.7B music-planner), not as a censorship workaround; README will state measured behavior.
- 2026-06-12 **Client must strip `<|audio_code_NNNNN|>` artifacts** from LM plan lyrics (observed once in `/v1/create_sample` output).
- 2026-06-12 **`wav` (16-bit PCM 48 kHz, ffprobe-verified) is the engine-request format** — master + WAV download; mp3 via our ffmpeg encode.
- 2026-06-12 **Measured perf recorded in ENGINE_NOTES §9** (44 s cold boot; 22.4 s/song batch-2 30 s; 29.7 s for a 180 s song) — these are the only numbers the README may cite.
- 2026-06-12 **M1 resolved versions (spec §3.1):** next 16.2.9 · react 19.2.4 · typescript 5.9.3 · tailwindcss 4.3.0 · drizzle-orm 0.45.2 · drizzle-kit 0.31.10 · better-sqlite3 12.10.0 (native build OK on Node 26.3.0) · zod 4.x · shadcn 4.11 (style `radix-nova`, base color neutral, CSS variables) · pnpm 11.6.0. Exacts in `pnpm-lock.yaml`.
- 2026-06-12 **shadcn CLI changed since spec:** `-b` now picks the primitive library (radix|base), themes come from presets; initialized with `-d -b radix` → radix-nova. shadcn injected Geist via next/font (build-time fetch, self-hosted at runtime) — kept.
- 2026-06-12 **pnpm 11 `allowBuilds` map in pnpm-workspace.yaml** approves native postinstalls (better-sqlite3, sharp, esbuild, @tailwindcss/oxide, unrs-resolver) — the package.json `pnpm` field is no longer read.
- 2026-06-12 **DB migrations auto-apply on first connection** (lib/db) in addition to `pnpm db:migrate` in setup.sh — dev works even without running setup.
- 2026-06-12 **M1 reviewed by adversarial workflow (16 confirmed findings, all fixed):** notably the unanchored `engine/` gitignore was silently excluding `lib/engine/` from commits; health probe now maps TimeoutError→"starting" (lazy-init hang ≠ offline); env URLs strip trailing slashes; scripts source `.env` and kill process groups on shutdown.
- 2026-06-12 **Ollama provisioning is instructions-only in setup.sh** (deviation from spec §10 "install + pull"): an unprompted brew install + ~5 GB model pull is hostile, and `ollama pull` needs the daemon running — setup prints exact commands instead; feature degrades gracefully per §6.2.
- 2026-06-12 **demucs pinned: `uv tool install --python 3.12 --with "torchaudio<2.9" --with soundfile demucs==4.0.1`** — unpinned resolution picks torchaudio 2.11 whose `load`/`save` APIs were removed (TorchCodec migration), breaking demucs audio I/O; <2.9 resolves torch 2.8.0/torchaudio 2.8.0; **soundfile** required or torchaudio has zero backends on macOS and stem save fails.
- 2026-06-12 **M2 lyric generation verified LIVE via Ollama qwen3:8b** (user's existing model; 19 s, structure-tagged, think-blocks stripped) — spec's "live if Ollama is present" satisfied; mock also covered in tests.
- 2026-06-12 **Queue boot recovery runs from `instrumentation.ts`** (spec §8 names it) — a restarted server resumes queued jobs with no API hit; verified live.
- 2026-06-12 **Variations are one atomic forge:** two-phase materialization (download ALL takes, then insert ALL rows); any download failure rolls back files and fails the job — no half-grouped variations in the library.
- 2026-06-12 **Reference-audio upload deferred to post-MVP** (spec §9.2 conditioned it on M0 confirmation): the REST layer supports multipart (source-verified) but it was never live-tested in M0 and needs a multipart path through our queue/EngineClient — logged for §14, keeps M3 on the core flow.
- 2026-06-12 **Style tags append to the prompt** — the engine has no separate tags field (M0); Advanced "Style tags" input joins the prompt as comma fragments (which also feeds songs.tags derivation).
- 2026-06-12 **Library page ships as an M3 stub** (plain list + native audio elements) so the forge flow has a visible destination; M4 replaces it with the real grid/art/players.
- 2026-06-12 **M3 review blocker fixed — Simple-tab vocal forges now use engine Simple Mode** (`sample_mode` + `sample_query`): empty lyrics would otherwise render an instrumental (engine treats "" as instrumental). Live-verified: LM wrote real verse/chorus lyrics. The LM's plan replaces request metas wholesale, so **Simple-Mode duration is LM-chosen**; a textual length hint in the query helps but is approximate (live: 30 s request → 153 s song) — documented behavior, not faked.
- 2026-06-12 **LM token artifacts stripped from ALL engine text outputs** (generation results, not just plan endpoints) — live Simple-Mode run leaked `<|audio_code_N|>` into stored lyrics before the fix.
- 2026-06-12 **M3 reviewed by adversarial workflow (21 confirmed findings, all fixed):** beyond the blocker — inverted queue positions, Enhance gated on `llmInitialized`, Unicode key normalization (LM emits "E♭ major"), Enhance preserves user-chosen fields and passes constraints, client-side clamping so free-text input can't 400 the strict schema, zod issues surfaced in toasts, Select/variations a11y, e2e builds first + asserts audio actually serves.
- 2026-06-12 **React-hooks v6 purity rules enforced by eslint-config-next:** no Date.now()/ref reads in render, no sync setState in effects — queue-strip shows active + 2 most recent terminal jobs (pure display rule), banner clock ticks async-only.
- 2026-06-13 **License changed to GNU GPL v3.0-or-later** — Wavesmith app source; ACE-Step remains a separate MIT upstream dependency (cloned at setup, not vendored).
- 2026-06-13 **Agent docs:** canonical instructions in `AGENTS.md` (Cursor + Grok Build); `CLAUDE.md` is a legacy pointer.
- 2026-06-13 **Authorship hard rule:** all commits, code, and packaging attribute **Sirius T.Bontea** `<sirius.bontea@protonmail.com>` only — no AI author/co-author trailers or credits (`.cursor/rules/authorship.mdc`, `AGENTS.md`).
- 2026-06-13 **M4–M7 implementation:** library grid/list + global Zustand player + wavesurfer song view; Demucs stems queue + ZIP; settings + skippable onboarding tour; README/LICENSE/smoke.sh/LINUX_CUDA.md; zustand 5.0.14 · wavesurfer.js 7.12.8 · motion 12.40.0 added.
- 2026-06-17 **MIDI export:** approximate transcription via Spotify Basic Pitch CLI (`--model-serialization onnx`); pinned `setuptools<81`, `scipy==1.11.4`, `onnxruntime` — TF saved models break on Py3.12; `midi_tracks` table + `midi` queue job; master or per-stem source.
- 2026-06-13 **Stems mock path:** when `MOCK_ENGINE=1`, demucs is skipped — master audio is copied to four stem WAVs so tests/e2e stay fast without the demucs binary.
- 2026-06-13 **Multilingual forge prep (items 1–3):** queue splits labeled/`[verse]` blocks out of empty-lyrics prompts; script detection resolves `vocal_language` (mixed → `unknown`); explicit or detected codes send `use_cot_language:false` — true per-section code-switching still unsupported by ACE-Step.
